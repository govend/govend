language: go

go:
  - 1.5
  - 1.5.1
  - tip

env:
  - GO15VENDOREXPERIMENT=1

before_install:
  - rm -rf vendor
  - go vet -v ./...

install:
  - go get -u github.com/gophersaurus/govend
  - govend -v

script: go test $(go list ./...|grep -v vendor)
